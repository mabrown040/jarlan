.DS_Store
node_modules
.next
out
coverage
.turbo
.tools
.cache
.claude
*.log

# Env secrets — Sentry DSN/token, Plausible config, etc.
# `.env.example` is committed so contributors know the shape.
.env
.env.local
.env.*.local
.sentryclirc
.sentry-build-plugin
.cursor

# Incremental compile cache — regenerated on every `tsc` run. Not
# meaningful history and it keeps dirtying unrelated PRs.
tsconfig.tsbuildinfo
